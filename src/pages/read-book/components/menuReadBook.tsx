import { Button } from '@/components/common/Button';
import { useStore } from '@/hooks/useStore';
import { observer } from 'mobx-react-lite';

export default observer(function MenuReadBook() {
    return (
        <div>
            123123
        </div>
    );
});
